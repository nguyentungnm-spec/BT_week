# Phân tích đối tượng - Bài 1.3
## 1. Lớp BankAccount
- **Trạng thái**: Các biến `accountNumber`, `ownerName`, `balance`.
- **Hành vi**: Các phương thức `openAccount()`, `deposit()`, `withdraw()`.
- **Định danh**: Tên biến đối tượng `myAccount`.
## 2. Lớp SmartFan
- **Trạng thái**: Các biến `brand`, `isPowerOn`, `speedLevel`.
- **Hành vi**: Các phương thức `turnOn()`, `turnOff()`, `setSpeed()`.
- **Định danh**: Các biến đối tượng `livingRoomFan` và `bedroomFan`.